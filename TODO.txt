trigger general cancel
    launchpad event loop
        emit general cancel when 0x78
            confirm lp input
            confirm lp input dispatch
            confirm emitted to aeolus
                constant music playing
                turn stop on command (text)
        

make gtest optional (-lgtest -lfmt would fail)
    vendor them?
    tup generate just build linux (not tests)
        linux/Tupfile or maybe tup options

