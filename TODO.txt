release v0.1
    cleanup
        make gtest optional (-lgtest -lfmt would fail)
            vendor them?
            tup generate just build linux (not tests)
                linux/Tupfile or maybe tup options
    distro stuff
        test with fresh rpi installation

set preset
    stops lighted
        model holds stops
        model sends diff of stops
    set+preset
        preset db
        update stops

## Notes
bwv651.mid plays on two channels (pedal on 0, manual on 1)
pmidi -p 128:0 ~/bwv651.mid